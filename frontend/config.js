// Frontend configuration.
// Set this to your deployed backend URL before deploying to Vercel, e.g.:
//   window.TMS_API_BASE = "https://traffic-monitoring-api.onrender.com";
// Leave as localhost for local development.
// (The value is also editable at runtime via the "Backend connection" field,
//  which is remembered per browser.)
window.TMS_API_BASE = "http://127.0.0.1:8000";
